version: '3'
services:
  app:
    restart: always
    #build: .
    image: ncr.pub/nyaone/email2misskey:latest
    networks:
      - internal_network
      - external_network
    ports:
      - "25:25"
    volumes:
      - "./config.yml:/app/config.yml:ro"

  redis:
    restart: always
    image: redis:alpine
    deploy:
      resources:
        limits:
          memory: 256M
    networks:
      - internal_network
    volumes:
      - ./redis:/data
networks:
  internal_network:
    internal: true
  external_network:
